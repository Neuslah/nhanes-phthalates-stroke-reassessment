# Outputs directory

This directory may contain aggregate, non-participant-level audit examples. New reproduction outputs should be written to the explicit output directory supplied at runtime, outside the release-candidate tree. Participant-level RDS/XPT files must never be placed here or committed to a public repository.
